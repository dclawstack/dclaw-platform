apiVersion: v1
kind: Service
metadata:
  name: dpanel-api
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dpanel-api
    app.kubernetes.io/part-of: dclaw-platform
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 8088
      targetPort: http
      protocol: TCP
      name: http

  selector:
    app.kubernetes.io/name: dpanel-api
